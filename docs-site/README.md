# ChillGroup Docs Site

Portal de documentacio basat en VitePress per publicar el contingut de `definitions/` a GitHub Pages.

## Comandes

```bash
pnpm install
pnpm docs:dev
pnpm docs:build
```

El build sincronitza automaticament els Markdown de `definitions/` dins la seccio catalana de referencia.